חלק ראשון: חיבור החיפוש לפי הדרישות

מקורות: 2.txt, SQL/3.txt ו-targets_dataset.sql, בגרסת המאגר e34aa5d.
SQL/3.txt משמש כאן כתיאור של הקוד החדש שכתבת. קובצי ה-Python במאגר עדיין כוללים גם את שלד המכשירים הקודם.
ההנחיות מיועדות לקבצים בשמות שמופיעים בדרישות ובתיאור שלך:
client/target_search.py, client/http_client.py, client/output.py, client/config.py
server/db.py, server/target_service.py, server/main.py
אם הקוד אצלך נמצא עדיין בשמות input.py, api.py, database.py, service.py, שנה את שמותיהם בהתאמה לשמות שלמעלה בפרויקט המבחן. השתמש בקוד המתואר ב-SQL/3.txt ובתיקונים להלן.
אין לערבב כאן ייבואים שמתחילים ב-SQL_API עם הייבואים הישירים שבדוגמה שלך.

תחילה שני קובצי main שביקשת. בהמשך התיקונים ההכרחיים בקוד שסביבם.
הקובץ עוסק ב-Feature 1 בלבד. לחלק השני אמתין להנחיה שלך.

1. client/main.py
פעולה: להחליף את כל הקובץ.

import requests
from target_search import choose_search_type, read_target_id, read_target_name
from http_client import request_last_location
from output import show_last_location


def main():
    while True:
        try:
            search_type = choose_search_type()

            if search_type == "id":
                search_value = read_target_id()
            else:
                search_value = read_target_name()

            result = request_last_location(search_type, search_value)
            show_last_location(result)

        except requests.exceptions.HTTPError as error:
            try:
                message = error.response.json().get("detail", str(error))
            except ValueError:
                message = str(error)
            print(f"Error: {message}")

        except requests.exceptions.RequestException as error:
            print(f"Connection error: {error}")

        except (KeyboardInterrupt, EOFError):
            break


if __name__ == "__main__":
    main()


2. server/main.py
פעולה: לכתוב בקובץ הריק שתיארת ב-SQL/3.txt.
אם אתה מעתיק לשלד הישן, זהו תוכן חלופי עבור Feature 1 בלבד; הוא אינו כולל את נתיבי הרישום והאימות מהשלד.

from fastapi import FastAPI, HTTPException
from target_service import find_last_location

app = FastAPI()


@app.get("/targets/last-location")
def get_last_location(
    target_id: int | None = None,
    name: str | None = None,
):
    if target_id is not None and name is not None:
        raise HTTPException(
            status_code=400,
            detail="Send either target_id or name, not both",
        )

    if target_id is not None:
        if target_id <= 0:
            raise HTTPException(
                status_code=400,
                detail="Target ID must be greater than zero",
            )
        result = find_last_location("id", target_id)

    elif name is not None:
        name = name.strip()
        if not name:
            raise HTTPException(
                status_code=400,
                detail="Target name cannot be empty",
            )
        result = find_last_location("name", name)

    else:
        raise HTTPException(
            status_code=400,
            detail="Send target_id or name",
        )

    if result is None:
        raise HTTPException(
            status_code=404,
            detail="Target not found",
        )

    return result


תיקונים הכרחיים כדי ששני קובצי main יעבדו

3. client/http_client.py
פעולה: להחליף את כל הקובץ בקוד הבא.
החתימה הנוכחית שלך מקבלת endpoint ו-params. הדרישה היא search_type ו-search_value, ולכן גם main וגם הפונקציה צריכים להשתמש בחתימה הבאה.

import requests
from config import SERVER_URL


def request_last_location(search_type: str, search_value) -> dict:
    if search_type == "id":
        params = {"target_id": search_value}
    elif search_type == "name":
        params = {"name": search_value}
    else:
        raise ValueError("Search type must be id or name")

    url = SERVER_URL + "/targets/last-location"
    response = requests.get(url, params=params, timeout=10)
    response.raise_for_status()
    return response.json()


4. server/target_service.py
פעולה: להחליף את כל הקובץ.
בשאילתה שבתיאור שלך חסרים FROM, JOIN ו-WHERE. נבחרו ארבעה שדות, אבל מילון התוצאה משתמש בחמישה. כאן מוחזרים חמשת השדות הנדרשים, ושמותיהם תואמים לפלט.

from db import db


def find_last_location(search_type, search_value):
    if search_type == "id":
        condition = "t.id = %s"
    elif search_type == "name":
        condition = "t.name = %s"
    else:
        raise ValueError("Search type must be id or name")

    query = f"""
        SELECT
            t.id,
            t.name,
            l.name,
            l.country,
            l.description
        FROM targets AS t
        JOIN locations AS l
            ON t.last_known_location_id = l.id
        WHERE {condition}
        LIMIT 1
    """

    with db.connection_context():
        cursor = db.execute_sql(query, (search_value,))
        row = cursor.fetchone()

    if row is None:
        return None

    return {
        "target_id": row[0],
        "target_name": row[1],
        "location_name": row[2],
        "country": row[3],
        "description": row[4],
    }


5. client/target_search.py
פעולה: שלושה תיקונים נקודתיים בקוד שכתבת.

א. בתוך read_target_id, להחליף את שורת ההחזרה האחרונה:
return value
בשורה:
return number
לשמור את אותה ההזחה. כך מוחזר int כפי שנדרש.

ב. בתוך read_target_name, להחליף את שורת ההחזרה האחרונה:
return value
בשורה:
return value.strip()
לשמור את אותה ההזחה.

ג. בתוך שתי הפונקציות, להחליף .ask() ב-.unsafe_ask().
כלומר:
value = questionary.text("Enter ID:").unsafe_ask()
וגם:
value = questionary.text("Enter name:").unsafe_ask()
כך Ctrl+C מטופל ב-main ולא הופך ל-None שעלול לגרום לשגיאה.
אין צורך לשנות את choose_search_type.

6. client/output.py
פעולה: להחליף את הפונקציה show_last_location בפונקציה הבאה, כדי לכלול גם את שורות הפתיחה והסיום הנדרשות.

def show_last_location(result: dict) -> None:
    print("===== Results =====")
    print(f"Target ID: {result['target_id']}")
    print(f"Target name: {result['target_name']}")
    print(f"Last known location: {result['location_name']}")
    print(f"Country: {result['country']}")
    print(f"Description: {result['description']}")
    print("!!!!! Done !!!!!")


7. הגדרות המסד והשרת

client/config.py ו-server/db.py יכולים להישאר כפי שכתבת ב-SQL/3.txt.
ב-client/.env:
SERVER_URL=http://127.0.0.1:8000

ב-server/.env: עדכן DB_NAME לשם המסד שיצרת בפועל ב-phpMyAdmin. שאר הפרטים חייבים להתאים ל-XAMPP שלך.
הפעל את שירות MySQL ב-XAMPP.
בחר את המסד שלך ב-phpMyAdmin וייבא אליו את targets_dataset.sql.
הקובץ אינו יוצר מסד בעצמו. הוא מוחק ויוצר מחדש את targets ו-locations, ולכן ייבא אותו למסד התרגול המיועד.
אין צורך להוסיף טבלאות חדשות עבור Feature 1.

8. הפעלה

הפקודות הבאות מתאימות לייבואים הישירים שמופיעים ב-SQL/3.txt ובקוד לעיל.
בטרמינל ראשון, מתוך תיקיית server:
python -m uvicorn main:app --reload --port 8000

בטרמינל שני, מתוך תיקיית client:
python main.py

אם ספרייה חסרה, התקן בסביבת הפייתון של הפרויקט:
python -m pip install fastapi uvicorn requests peewee pymysql python-dotenv questionary

9. בדיקה אצלך והתוצאה הצפויה

בחר חיפוש לפי ID והזן 1.
לאחר ייבוא קובץ הנתונים המצורף, הפלט הצפוי הוא:
===== Results =====
Target ID: 1
Target name: Cedar Falcon
Last known location: Nabatieh South
Country: Lebanon
Description: Area south of Nabatieh
!!!!! Done !!!!!

בחיפוש לפי name, הזן Cedar Falcon. צפויה אותה תוצאה.
ID שלילי או טקסט במקום מספר אמורים לגרום לבקשה להקליד שוב.
ID שאינו קיים, למשל 9999, אמור להציג Error: Target not found.
Ctrl+C יוצא מהלקוח.

בדיקה ישירה בדפדפן, כאשר השרת פועל:
http://127.0.0.1:8000/targets/last-location?target_id=1

ללא פרמטרים, או עם target_id ו-name יחד, צפויה שגיאת 400.
עם target_id שאינו מספר שלם, FastAPI אמור להחזיר 422.

מה נבדק כאן:
נבדק תחביר קטעי הקוד המלאים. נבדקה שאילתת החיפוש עם נתוני ה-SQL שצירפת במסד SQLite זמני בזיכרון, לאחר התאמת תחביר הייבוא ומצייני הפרמטרים. נבדקו חיפוש לפי ID, חיפוש לפי שם ומצב ללא תוצאה.
לא הורצה המערכת המלאה מול XAMPP/MySQL שלך ולא בוצעה בדיקת HTTP חיה, ולכן עדיין צריך לבצע אצלך את בדיקת ההפעלה שלמעלה.